### Tại sao lệnh if không đủ?

Lệnh if (rs.next()) chỉ kiểm tra xem có ít nhất một dòng dữ liệu trả về hay không. Nếu có, nó sẽ di chuyển con trỏ đến dòng đầu tiên, thực thi các lệnh bên trong khối if rồi kết thúc.