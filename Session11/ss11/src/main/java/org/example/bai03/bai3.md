### Giá trị trả về của executeUpdate()
- Giá trị trar về của executeUpdate() là một số nguyênkeeir int tượng trương cho số dòng bị thay đổi trong database
- Để phản hồi chính xác cho y tá ta kiểm tra kết quả trả về của hàm executeUpdate() nếu = 0 thi in ra mã giường không tồn tại
